/* ==========================================================================
   zara-tracking — Runtime (auto-binder)
   ========================================================================== */

export { runTracking } from "./auto-binder";
export type { RuntimeHandle } from "./auto-binder";

export { dispatch, buildContext, resetDispatchGuard } from "./dispatch";
export type { DispatchInput } from "./dispatch";
