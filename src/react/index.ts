export { MetaPixel, default } from "./MetaPixel";
export type { MetaPixelProps } from "./MetaPixel";

// TrackingProvider is a Client Component — import from "zara-tracking/react/provider"
// (separate entry so the "use client" directive survives bundling).
